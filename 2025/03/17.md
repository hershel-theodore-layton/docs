# 2025-03-17

## Future Changes

- During the staged class-pointer migration, `__DynamicallyReferenced` accepts an optional integer literal for additional notice sampling. When HHVM's global dynamically-referenced notice sampling selects a string-based class load, `<<__DynamicallyReferenced(N)>>` applies a further roughly 1-in-`N` check; `0` suppresses the soft notice. The argument must be a single integer literal, while the argument-free attribute retains its normal fully opted-in behavior. [74e1b401c2](https://github.com/hershel-theodore-layton/hhvm/commit/74e1b401c2aec729dc774b6cb26ab676d28257fb)
