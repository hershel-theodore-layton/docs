# Commit 000637

Commit hash: 2fba55e2b78fc0b48a14fbf3a09342cdb961c990
Commited at: 2022-11-28T03:12:32-08:00
Previous commit hash: bb878116c3a528e374d7f25e4459f1656d22e00a
Next commit hash: 5710ce832c12801a5d4c1c9a00863d5847fa3c52
Commit title: hash more ast bits
Commit body:
Summary:
Before this diff, we hashed only the symbol occurrence name and symbol occurrence types. This wasn't enough to fully characterize the indexer output for `hack.FileXRef`. We also need to use the *occurrence definition* `full_ name` which what ultimately determines the target in this case.

Consider symbol `getProdPolicy` in https://www.internalfb.com/code/www/[0012fdcedb3d]/flib/purpose/cipp/wrapper/ZonedValue.php

```
  <<__Override>>
  public async function genUnwrapX()[zoned_shallow]: Awaitable<T> {
    $can_unwrap = await $this->genCanUnwrap(null);

    if (!$can_unwrap) {
      PolicyZoneUtils::throwCIPPFlowsToViolationException(
        $this->getContextBuilder()
          ->getProdPolicy()
          ->get()
          ->getSourcePolicySet(),
        CIPPDataType::WRAPPER,
        $this->dataClass as string,
        CIPPEventType::DATA_FLOW_IN,
      );
    }
    return $this->getValue()->getResult();
  }
```

In revision 2850583afce698f58a05b, it points to `__CIPPFlowsToComparableContextBuilder::getProdPolicy`.

- occurrence.name `\__CIPPFlowsToComparableContextBuilder::getProdPolicy`
- occurrence type: `(SymbolOccurrence.Method ("getProdPolicy"))`
- definition.full_name: `__CIPPFlowsToComparableContextBuilder::getProdPolicy`

In revision df73d7618ec3ed8c91809, it points to `__CIPPFlowsToComparableBuilder::getProdPolicy`

- occurrence.name `\__CIPPFlowsToComparableContextBuilder::getProdPolicy`
- occurrence type `(SymbolOccurrence.Method ("getProdPolicy"))`
- definition.full_name `__CIPPFlowsToComparableBuilder::getProdPolicy`

Reviewed By: donsbot

Differential Revision: D41404740

fbshipit-source-id: fdb944d9532741c20d888c9e56d3d8752471b60a
Touched files:
- hphp/hack/src/typing/write_symbol_info/symbol_file_info.ml
