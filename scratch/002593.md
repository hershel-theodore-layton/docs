# Commit 002593

Commit hash: f6f8556d1b7db8ebaaf4b1323999671561e33767
Commited at: 2023-02-22T19:07:40-08:00
Previous commit hash: e0542eb8fba8fd92d5bb33063ab8f220ba57e4ef
Next commit hash: 40c2c4747d8241c8c46e669fe8d9a24936a0aeb1
Commit title: use env, not runtime_deps+codegen
Commit body:
Summary:
Our integration tests have historically been a bit flakey, usually manifest as "binary not found" on CI.

I think the root problem is the magic we do with gen_hh_paths. Imagine that one machine builds the :hh_paths target (i.e. executes `gen_hh_paths.py $(location :hh_fanout)` to produce hh_paths.py, which looks like this:
```
# generated hh_paths.py:
hh_fanout = "/data/users/ljw/fbsource/buck-out/v2/gen/fbcode/5a0257a8720f68cd/hphp/hack/src/hh_fanout/__hh_fanout__/hh_fanout.opt"
```
Some people claim that $(location) is meant to produce repo-relative paths, but from what I've seen it produces absolute paths. In any case, the final hh_paths.py artifact certainly includes absolute paths.

Now a different machine is tasked to build and run the unit test. The test target looks like this:
```
    runtime_deps = [
        ":hh_paths",
        "//hphp/hack/src/hh_fanout:hh_fanout",
    ],
```
It will pick up hh_paths.py from CAS, embodying the absolute path used on that other earlier machine. It will pick up the hh_fanout binary also from CAS, perhaps on a quite different absolute path. I believe this is the root cause of our flakiness.

## What to do?

I could have changed gen_hh_paths to embed repo-relative-paths in hh_paths.py, so that they only get turned into absolute paths at runtime.

But think we have too much magic going on. So instead this diff simplifies things. Each unit-test now declares the dependencies it needs in environment variables
```
    env = {
        "HH_FANOUT": "$(exe //hphp/hack/src/hh_fanout:hh_fanout)",
    },
```
And hh_paths.py doesn't have any codegen. It merely reads the environment variable and validates that a binary is there properly.

The validation means that the tests will fail-fast if someone forgot to set the environment variable in the test's target. (By contrast, with the runtime_deps mechanism, the test would only sometimes fail if you forgot to set a runtime_dep).

I also put a great deal of care into hh_server and hh_client binary locations. I explained in the comment in hh_paths.py:
```
# GOTCHA 1. clientStart.ml requires that hh_server be in the same directory has hh_client.
# Also, part of the python tests set HH_HOME=dirname(hh_client) and then invoke $HH_HOME/hh_server.
# Recall that we're using the side-effect that :hh_server places a symlink into the right directory.
# (1) We will use "dirname($location :hh_client)/hh_server" as our path to HH_SERVER
# (2) We will still expect the unit test to set "HH_SERVER=($location :hh_server)" so they don't
# forget to have a dependency on :hh_server and its side-effect. Note that :hh_server has labels
# "uses_local_filesystem_abspaths" and "writes_to_buck_out" so it will be run on the same machine
# as the unit-test, i.e. will create its symlinks on that machine.
# (3) We'll ensure that the two paths to hh_server point to identical binaries, to avoid the case
# where an old symlink happened to be left behind.
```

## What about buck1?

I just disabled the tests under buck1, under guidance from the buck team https://www.internalfb.com/intern/qa/429741/location-failing-to-bring-in-dependencies

Reviewed By: edwinsmith

Differential Revision: D43383604

fbshipit-source-id: 7d5b85c5e2570ca11050f6f0f03f108627b7a1ec
Touched files:
- hphp/hack/test/integration/common_tests.py
- hphp/hack/test/integration/hh_paths.py
- hphp/hack/test/integration/test_lsp.py
