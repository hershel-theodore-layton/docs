# Commit 003689

Commit hash: 6af7c50224eaae01e1bba130f56528f402e5ef90
Commited at: 2023-03-30T19:22:40-07:00
Previous commit hash: c650da95c4afe57eef72e4d224144d08d3454ae1
Next commit hash: fc4279b6b07aea320600b9451c79ae0a79a6230a
Commit title: 2/3 server produces streaming errors
Commit body:
Summary:
With this diff, the server will now report streaming errors to an errors.bin file. (consuming this file by the client is left to the next diff in the stack).

My rollout plan is
1. The JK `produce_streaming_errors` will roll out fairly quickly. Most machines will be writing to errors.bin. There will be some slight overhead to this. The JK is here to mitigate the risk that the new "writing" codepaths introduce instability in hh_server, or validation is too costly.
2. A separate JK `ide_standalone` (or `hh --config ide_standalone=true`) will control whether hh_client should read errors from errors.bin or via the existing ServerRpc.STATUS handler. This rollout will change observable behavior. I plan to do the rollout with volunteers first.

## Concurrency and lifecycle semantics

```
LIFECYCLE SEMANTICS OF PRODUCING AND CONSUMING THE ERRORS-FILE

The code that produces errors-file lives in ServerMain.ml (which registers an on-exit
hook to delete the file), ServerTypeCheck.ml (to manage the previous and new
errors-file immediately before and after a typecheck is performed),
and typing_check_service.ml (to make the actual error reports).
1. When hh_server is launched, it either eventually exits or eventually writes
an errors.bin file with some clock value that came at or after its launch.
2. When files on disk are changed that pass FilesToIgnore.watchman_server_expression_terms
and FindUtils.post_watchman_filter, then eventually either a new errors.bin will
be written which reflects the clock after those files, or eventually it will terminate.

These invariants imply how the client should connect:
1. If there's no errors.bin, then doing an RPC connect to the server that succeeds
means that it's fine to just wait around waiting for an errors.bin to succeed.
(Except for one vulnerability: if the server handles the RPC but then crashes
before starting its first check).
2. If there is an errors.bin but files have changed on disk since error's watchclock,
it's fine for the client to just wait until a new errors.bin gets created.
```

These lifecycle semantics produce a design where the client "just waits around" waiting for a suitable errors.bin file to appear. I find this unsatisfying - e.g. in error cases where a suitable errors.bin doesn't eventually appear then it's hard to debug. But the alternate design would have been too disruptive. I've left the discussion on this topic to the next diff, where we can read the consumer code as well.

## Three different errors models

```

  (*
  (1) THE ENV MODEL FOR ERRORS...
  env.{errorl, needs_recheck, disk_needs_parsing} are all persistent values that
  might be adjusted as we go:
  * disk_needs_parsing gets initialized in serverLazyInit, augmented in serverMain both
    at the start of the loop and during watchman interrupts, and in serverTypeCheck it
    gets reset to empty once we have computed decls-to-refresh from it.
  * needs_recheck gets augmented in serverTypeCheck from fanout/stale computation,
    and gets discharged by the files we end up typechecking
  * errorl starts out empty and it grows+shrinks during serverTypeCheck
    through calls to "errorl = Errors.incremental_update ~errorl ~new_errors ~phase ~files_examined".
    This will shrink those errors that had been in errorl before, and were in files_examined,
    but are not in new_errors. It will replace others. It will grow others.
    And it will leave remaining in errorl anything that was not touched by files_examined
    or which came from a different phase.
    To stress, say you have 10k errors and make a small change in one file,
    it is very possible that those 10k other files are not checked and the bulk of errorl
    just continues through a recheck loop. (However, we do gather every single file
    mentioned in any of the *reasons* of those 10k files, and where those reasons intersect
    with changed-files then that causes need to redecl and compute fanout, and also need
    to recheck.)

  (2) THE DIAGNOSTICS_PUSHER MODEL FOR ERRORS...
  This is used for the persistent connection. It maintains its belief of what the persistent
  client already knows, and pushes deltas. The less said about it, the better.

  (3) THE STREAMING MODEL FOR ERRORS...
  The errors-file grows monotonically: it has no "backsies", no way to remove an error
  from it, short of deciding that the current errors-file is wrong and a new error-file must
  be restarted.
  * Right here at the start of the type check, we restart the errors-file (because of the
    potential that in the current typecheck we discover that some files no longer have errors).
  * The typecheck fulfills the contract that every single error must be reported to the errors-file.
    Not just newly discovered errors. Every error, even those from files that do not get rechecked.
  * Right here at the end of the type check, if the type check was complete then we need to report
    this fact right away in the errors-file so that the client "hh check" can finish "No errors!".
    But if it was not complete (e.g. it got interrupted by watchman) then there is no need to finish
    for the sake of the client: there will be an immediate next round of
    ServerMain.recheck_until_no_changes_left, and it will call us again, and the errors-file
    will be restarted on that next round, and the act of restarting will close the current errors-file.
  *)
```

The logic for `ServerTypeCheck.push_errors` and `Diagnostic_pusher.push_new_errors` is quite complicated. It has the job of pushing errors over the persistent connection. It's complicated because it must deal with
1. it has to remember which files previously had errors so it knows whether to reset them or not
2. it has to deal with multiple phases, e.g. errors that arose during parsing, during naming, and during typechecking; if it pushed some errors from one phase for A.PHP, and the next phase records an all-clear, it must combine them together, and must certainly not push an empty list for A.PHP

But with streaming errors I was able to produce something simpler.
1. The errors.bin file never need worry what errors are already out there, because at the start of a typecheck it's by definition empty.
2. rjbailey has rolled out direct-decl-parser and deleted the old codepaths. Using the direct-decl-parser, `do_parsing` now will never produce errors. So I didn't bother with errors from this phase. See D44113662 for the start of a diffstack to clean up the corresponding code in ServerTypeCheck.ml
3. My next project will be to remove errors from the naming phase as well. That will leave solely the typechecking phase that can report errors.
4. In the meantime, I specified a different semantics for the errors.bin file. I said it's allowed to have errors for A.PHP reported at one stage, and then additional errors for A.PHP reported at a later stage. For CLI consumers of streaming errors this is no worry - they'll happily and correctly display both. For LSP, I think for now I'll just have the simple rule that if naming produced errors for A.PHP then I'll ignore subsequent typecheck errors for A.PHP. That's easy to implement and avoids the problem.

The fact that this "ServerProgress.Errors.report" model is so different from (1) `env.errorl`, (2) `ServerTypeCheck.push_errors` is what made me want to add validation that it's correct. Hence, at the end of typechecking, I added a call to `ServerProgress.Errors.validate_DELETE_THIS_SOON`

The docblock above mentions: "The typecheck fulfills the contract that every single error must be reported to the errors-file. Not just newly discovered errors. Every error, even those from files that do not get rechecked." This is implemented by the function `ServerTypeCheck.push_to_errors_file`.

This diff adds telemetry to record any differences between the errors reported in the errors-file, vs those (authoritative) ones in env.errorl. I put telemetry:
* in the logfile. It will show "good news!" for each typecheck where errors-file is correct, and if any one isn't then it'll display that
* in a new event ERRORS_FILE_MISMATCH which has details on differences, and shows error codes
* also added to RECHECK_END is a simple string "good" or "extra items" or "absent items".

## How to read this diff

1. Read the two docblocks in the summary
2. The docblock for "(3) streaming error model" talks about two invariants: (A) starting a new errors file and completing it, which is implemented at the bottom of `ServerTypeCheck.ml`, so read that first; (B) the invariant that type_check must report every single error, so read the remainder of the changes to `ServerTypeCheck.ml` and also `typing_check_service.ml` to see how that is implemented.
3. Read `serverMain.ml`. This is just five lines of code to set things up.
4. Read `ServerProgress.validate_DELETE_THIS_SOON` - but this is only validation, so it's not crucial
5. There are two new test cases in `server_progress_tests.ml`. Also, I edited existing tests so they bypass HHI generation+parsing, so they run faster.
6. The rest of the changes are just plumbing - JK, and telemetry.

Reviewed By: CatherineGasnier

Differential Revision: D44159584

fbshipit-source-id: 4e8593769a321c09db9a3f78df55b49d31cc5a51
Touched files:
- hphp/hack/src/server/serverLocalConfig.ml
- hphp/hack/src/server/serverMain.ml
- hphp/hack/src/server/serverProgress.ml
- hphp/hack/src/server/serverProgress.mli
- hphp/hack/src/server/serverTypeCheck.ml
- hphp/hack/src/stubs/logging/hackEventLogger.ml
- hphp/hack/src/typing/typing_check_service.ml
- hphp/hack/test/integration_ml/server_progress_tests.ml
