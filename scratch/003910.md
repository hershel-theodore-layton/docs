# Commit 003910

Commit hash: 50c95e9e31d9d71108ed372c10e9c537556cd3b1
Commited at: 2023-04-06T06:32:37-07:00
Previous commit hash: 86257e4aa0b9c5b14d1b9a394948eea068804de1
Next commit hash: 9329c78527d98d479489a6c73d3ca66021a39f4d
Commit title: telemetry to prove that Errors.Decl isn't used
Commit body:
Summary:
I have a hypothesis that Decl.make_env never actually creates any errors, and hence Errors.Decl phase is never actually used. This diff will provide telemetry to test the hypothesis.

Errors.Decl is only used in two places.
```
# decl_redecl_service.ml
let decl_file ctx errors fn =
  let (decl_errors, ()) =
    Errors.do_with_context fn Errors.Decl (fun () ->
        Decl.make_env ~sh:SharedMem.Uses ctx fn)
  in
  Errors.merge decl_errors errors

# decl_service.ml
let decl_file ctx errorl fn =
  let (errorl', ()) =
    Errors.do_with_context fn Errors.Decl (fun () ->
        Hh_logger.debug
          ~category
          "Typing decl: %s"
          (Relative_path.to_absolute fn);
        Decl.make_env ~sh:SharedMem.Uses ctx fn;
        Hh_logger.debug ~category "Typing decl OK")
  in
  Errors.merge errorl' errorl
```

Decl_redecl_service is called by ServerTypeCheck for the main typechecker loop. Decl_service is called by ServerEagerInit.

Reviewed By: CatherineGasnier

Differential Revision: D44736172

fbshipit-source-id: 44c35e0b2a4e5edd20d9f402ce7ef04ee1f55614
Touched files:
- hphp/hack/src/errors/errors.ml
