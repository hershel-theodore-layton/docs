# Commit 007555

Commit hash: 4b4090eaf601e6f5fdc0dfa5fe2f9278de35f965
Commited at: 2023-08-15T06:13:11-07:00
Previous commit hash: 2611786b77b082c961b6dad5f2dcfed134c590d3
Next commit hash: d3b6066fb8f0fe5e45b8c13bb5fa246e54e2c350
Commit title: Generic map-reduce framework on the OCaml side
Commit body:
Summary:
Implement a generic map-reduce framework for `hh_server`/`hh_distc`

This diff introduces the OCaml side:

```

(** A map-reducer maps a TAST into intermediate data, and then reduces a
    bunch of intermediate data to a single value. *)
module type MapReducer = sig
  (** Type of the intermediate and reduced data. *)
  type t

  (** A function that takes type checker options and returns whether the
      map-reducer is enabled *)
  val is_enabled : TypecheckerOptions.t -> bool

  (** A map function that takes in a TAST and produces the intermediate data.

  Note that we don't make guarantees about the uniqueness of the file path. *)
  val map : Relative_path.t -> Tast.by_names -> t

  (** Reduce two intermediate data elements. *)
  val reduce : t -> t -> t
end
```

and makes `tast_hashes.ml` conform to the signature.

It then adds `TastHashes` as a map-reducer:

```
type _ map_reducer_type = TypeForTastHashes : Tast_hashes.t map_reducer_type

let implementation_for (type t) (mr : t map_reducer_type) :
    (module MapReducer with type t = t) =
  match mr with
  | TypeForTastHashes -> (module Tast_hashes)
```

This next diff changes `hh_distc`/`hh_server` to start using this framework.

Reviewed By: andrewjkennedy

Differential Revision: D47400437

fbshipit-source-id: d4f56a50bd0d7e7ca4bbea9078f4968e1f520d20
Touched files:
- hphp/hack/src/typing/service/map_reduce.ml
- hphp/hack/src/typing/service/map_reduce.mli
- hphp/hack/src/typing/service/tast_hashes.ml
- hphp/hack/src/typing/service/tast_hashes.mli
