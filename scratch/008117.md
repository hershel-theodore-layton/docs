# Commit 008117

Commit hash: bc04f0feb40699962f14ca51e2f0bd3d15e3d2c3
Commited at: 2023-09-04T03:46:13-07:00
Previous commit hash: fcd74a2efa5194a0efb97f2660d13ab1b07ee675
Next commit hash: 081114124a2b92987a7e22850bb2bc64cbb8f781
Commit title: DepGraphWithDelta
Commit body:
Summary:
This diff adds a struct `DepGraphWithDelta` containing a base depgraph and a delta depgraph.

My primary motivation is to write unit tests for it, which I will do in the next diff.

All functions from deps_rust_ffi.rs using both `dep_graph_delta_with` and `dep_graph_map.*` have been re-written to use a new helper `lock_depgraph_and` in typing_deps.rs with signature:
```
fn lock_depgraph_and(..., f: FnOnce(DepGraphDelta) -> R)
```
This way, we can actually remove some of the locking functions in typing_deps and reduce the API surface. Further reduction will be done in following diffs.

Trade-off: `depgraph_has_edge_for_sure` now gets both locks at the same time instead of one by one. But 1) the code is cleaner, 2) it shouldn't matter if we use RwLock instead of Mutex as we do in D48605477.

We could also "move" more logic from deps_rust_ffi.rs in future diffs.

Reviewed By: nt591

Differential Revision: D48643649

fbshipit-source-id: 9be02789c912513d99f7aea01dda59d0d4c388a6
Touched files:
- hphp/hack/src/deps/deps_rust/dep_graph_with_delta.rs
- hphp/hack/src/deps/deps_rust/deps_rust_ffi.rs
- hphp/hack/src/deps/deps_rust/typing_deps.rs
