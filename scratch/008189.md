# Commit 008189

Commit hash: cd9b143e1c77c6b250108ba7fbf015ba55d0587b
Commited at: 2023-09-05T14:43:09-07:00
Previous commit hash: 42e4a39da8c99f71de97f85968469efb738d3d01
Next commit hash: 6f52eb248f67bc43a44e3f023a66b04f747d10e8
Commit title: (stresstest) some improvements regarding using io_uring
Commit body:
Summary:
Currently, in stress test server, set `--io-uring true` doesn't do anything.
two changes in this diff
1. if `FLAGS_io_uring` is set to true, set both `setPreferIoUring` and `setUseDefaultIoUringExecutor`

2. if `setUseDefaultIoUringExecutor` is configured, we set up `DefaultIOUringExecutor` in `configureIOUring` regardless of what is currently configured. We may want to revisit the config a bit to make it easier to set up.

For example, we may want to only use `FLAGS_io_uring` flag, and if true and `ioThreadPool_` is not set, we set the default one. Else, we check whether configured `ioThreadPool_` is compatible with io_uring. if not, we may want to crash as the server is not configured consistently rather than fall back to epoll.

Reviewed By: robertroeser

Differential Revision: D48885772

fbshipit-source-id: 768170fb6012a0a1031c8919317b4a8dcc214c45
Touched files:
- third-party/thrift/src/thrift/conformance/stresstest/server/StressTestServer.cpp
- third-party/thrift/src/thrift/lib/cpp2/server/ThriftServer.cpp
