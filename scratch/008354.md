# Commit 008354

Commit hash: ab2c8d7f86c8f421d55935497d2fb7d7e2439ed0
Commited at: 2023-09-08T10:58:57-07:00
Previous commit hash: 8e7602df66cc63d7759b62e6ceb1f37c410cf3d8
Next commit hash: c3045c650d9aa9e7e597e31cb52b01396441cda0
Commit title: Add client_methods_bypass_eventhandlers Thrift Flag
Commit body:
Summary:
This diff introduces a Thrift Flag where bypassed event handler client method names can be encoded in a single string. The doc block explains the semantics:
```
/**
 * This flag encodes a set of client method or service names.
 *
 * Each entry in the set is separate by a single comma, without surrounding
 * whitespace.
 *
 * Each entry can be one of:
 *   - Method name: a Thrift method name in the format
 *     "<service_name>.<method_name>"
 *   - Service name - a Thrift service name in the format "<service_name>.*"
 *     (must end in ".*")
 *
 * For each service name, client methods targeting that service name will not
 * invoke EventHandlerBase callbacks.
 *
 * For each method name, client methods targeting that method (in the contained
 * service) will not invoke EventHandlerBase callbacks.
 */
```

`DelayedInit` is used instead of `Singleton` because singletons are lazy and the object is not directly accessed.

Differential Revision: D48763403

fbshipit-source-id: ca98f325d436c06c0398dc957f413c128633a4a6
Touched files:
- third-party/thrift/src/thrift/lib/cpp2/detail/EventHandlerRuntime.cpp
- third-party/thrift/src/thrift/lib/cpp2/detail/EventHandlerRuntime.h
- third-party/thrift/src/thrift/lib/cpp2/detail/test/EventHandlerRuntimeTest.cpp
