# Commit 008874

Commit hash: 73356a63273486e890502673e5020ad02fa0101e
Commited at: 2023-09-29T11:02:34-07:00
Previous commit hash: 056e1bddddd93c5e240737144df8ed831b0161f4
Next commit hash: f1d205c898a725837fdbab4e019c45e5118e6dc0
Commit title: fix discrepancy when clear optional patch
Commit body:
Summary:
Today when clearing the patch in patch prior and ensuring it later, there is a discrepancy between static and dynamic patch, since clear is set to intrinsic default in dynamic patch, but remove the field in static patch.

```

  Opt foo;
  foo.field() = 10;
  auto dynFoo = protocol::asValueStruct<type::struct_t<Opt>>(foo);

  OptPatch patch;
  patch.patchIfSet<ident::field>().clear();
  patch.ensure<ident::field>(1);

  // Apply patch statically
  patch.apply(foo);
  EXPECT_EQ(foo.field(), 1);

  // Apply patch dynamically
  protocol::applyPatch(patch.toObject(), dynFoo);
  EXPECT_EQ(dynFoo.as_object()[FieldId{1}].as_i32(), 1);
```

This diff fixed the issue by using assign in patch after instead of ensuring it (so that patch after will always override the value even dynamic patch did not remove the field).

Reviewed By: thedavekwon

Differential Revision: D49354695

fbshipit-source-id: ee8ac5c5c821403af7785127ba7dd04d64fd76d3
Touched files:
- third-party/thrift/src/thrift/lib/cpp2/op/detail/StructPatch.h
- third-party/thrift/src/thrift/test/StructPatchTest.cpp
