# Commit 009813

Commit hash: a7c99d57c2c7b50db2a58d90e184ee01eadfd0ed
Commited at: 2023-11-09T11:52:38-08:00
Previous commit hash: 2de6cd5d0585b782540a4c0d8c10d605d8eefcf6
Next commit hash: 920d2a6e32d2b88c59b9773a92d6f30ecffe5d7b
Commit title: (java) migrate legacy reactor api usage subscriberContext -> contextWrite
Commit body:
Summary:
We use `subscriberContext` in generated clients, mostly for implementing interactions. We need to migrate this API to `contextWrite` to prepare for reactor-core 3.5.0 upgrade

in 3.4.32 (current version) documentation says

```
	/**
	 * Enrich a potentially empty downstream {link Context} by adding all values
	 * from the given {link Context}, producing a new {link Context} that is propagated
	 * upstream.
	 * <p>
	 * The {link Context} propagation happens once per subscription (not on each onNext):
	 * it is done during the {code subscribe(Subscriber)} phase, which runs from
	 * the last operator of a chain towards the first.
	 * <p>
	 * So this operator enriches a {link Context} coming from under it in the chain
	 * (downstream, by default an empty one) and makes the new enriched {link Context}
	 * visible to operators above it in the chain.
	 *
	 * param mergeContext the {link Context} to merge with a previous {link Context}
	 * state, returning a new one.
	 *
	 * return a contextualized {link Mono}
	 * see Context
	 * deprecated Use {link #contextWrite(ContextView)} instead. To be removed in 3.5.0.
	 */
	Deprecated
	public final Mono<T> subscriberContext(Context mergeContext) {
		return subscriberContext(c -> c.putAll(mergeContext.readOnly()));
	}
```

Reviewed By: prakashgayasen

Differential Revision: D50992742

fbshipit-source-id: b6f45533c6f7bb8fe4c474df5a409d15cc680cab
Touched files:
- third-party/thrift/src/thrift/compiler/generate/templates/java/ReactiveClient.mustache
- third-party/thrift/src/thrift/compiler/test/fixtures/basic-annotations/gen-java/test/fixtures/basicannotations/BadServiceReactiveClient.java
- third-party/thrift/src/thrift/compiler/test/fixtures/interactions/gen-java/test/fixtures/interactions/MyServiceReactiveClient.java
