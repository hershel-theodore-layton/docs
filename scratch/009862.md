# Commit 009862

Commit hash: 4e038b04064fb6d40084de99b5cdc38d40a91f10
Commited at: 2023-11-10T15:30:58-08:00
Previous commit hash: fbf5ebc0a5a2f8ac1053e0958867fa0111c00b40
Next commit hash: 766dce0c09a6b8da48ff8acf517e31a49b2a172a
Commit title: local backend doesn't use Decl_store for most decls
Commit body:
Summary:
Our decls are a bit of a mess. I believe that Decl_store isn't used for most decls. I've added "failwith" to indicate this.
```
  (* The decl APIs are a bewildering mess!
     1. [add_class] and [get_class] are how the class folder stores its stuff.
     2. shallow classes are retrieved and stored via the Shallow_decl_provider
     3. [add_tyepdef] and similar are not used for local backend;
     these are instead provided by Decl_provider directly.
     4. [add_method] and similar are caches used by the class folder for member caches.
     They're disabled for local-memory backend.
     I think it would have been better to disable those caches with [TypecheckerOptions.populate_member_heaps],
     but instead we're disabling them here by making cache writes be a no-op.
  *)
```

Since this diff passes CI, including all the LSP tests, I think I'm right!

Why am I doing this? I started by wanting to add a decl-hash into the local-memory backend for Decl_provider. I was thwarted by this additional place that puts things into the local-memory backend. This diff is my way of showing that this additional place isn't needed.

(I ended up coming up with a different plan, one which doesn't add decl-hashes, but I still think this is a good diff for the cleanup+clarity it brings.)

Reviewed By: rjbailey

Differential Revision: D51136124

fbshipit-source-id: 547c5bd3526d9ec18d2a68413b5157971280225d
Touched files:
- hphp/hack/src/providers/provider_backend.ml
