# Commit 010040

Commit hash: b02a822d298e322c60aa8c51f5c960ca6da907cd
Commited at: 2023-11-21T07:51:04-08:00
Previous commit hash: 59733f4c746bd705867de20dd576ab3b76a6be83
Next commit hash: b4581c10d63ecf9e8be2f26fea694cf8f1ae3375
Commit title: Optimised added parent fanout: traverse NotSubtype edges
Commit body:
Summary:
This diff and the one before and after aim at fixing the following edge case in the new added parent fanout algo:
```
// a.php
class A {}

// b.php
class B {}

// c.php
function f(A $a): void {
  if ($a is B) {
    // We refine the type of $a to A & B
    expect_nothing($a)
  }
}

function expect_nothing(nothing $_): void {}
```
When changing A:
```
class A extends B {}
```
`f` will not be rechecked by the fanout algo, even though it now has an error.

### What's really going on here?

The code above typechecks (before user change) using facts of a kind we haven't considered when deriving the algorithm: that A is not a subtype of B and B is not a subtype of A. So "is not subtype" kind of facts: not (t1 <: t2).

Obviously, adding a parent can invalidate some of these facts, so whatever typing derivation we did using those facts needs to be redone, and the file (we did the typing derivation for) needs to be included in the fanout.

### Fix

We can use a new kind of dependency edge `NotSubtype` whenever we use a 'is not subtype' fact in a typing derivation. So in the example above, we used not (A <: B) to derive that f typechecks, so we'd record a NotSubtype edge between dependent f and dependency A.

Then when adding a parent to some type X, we'd gather all the descendants of X via the Extends edges of the graph, then follow any NotSubtype edge from any of these descendants and include the end of those edges in the fanout.

### This diff
In this diff, upon adding a parent to type A, we traverse the NotSubtype edges of descendants of A and add the edges ends to the fanout. See doc comments in this diff.

Differential Revision: D51031414

fbshipit-source-id: 583d27a699874b3e65d363c98be9f2544f93d12c
Touched files:
- hphp/hack/src/decl/shallow_class_fanout.ml
- hphp/hack/src/deps/deps_rust/dep_graph_with_delta.rs
- hphp/hack/src/deps/deps_rust/deps_rust_ffi.rs
- hphp/hack/src/deps/typing_deps.ml
- hphp/hack/src/deps/typing_deps.mli
- hphp/hack/test/fanout/series_of_repo_changes/added_parent_disjoint_interect1.php
- hphp/hack/test/fanout/series_of_repo_changes/added_parent_disjoint_interect1.php.exp
- hphp/hack/test/fanout/series_of_repo_changes/added_parent_disjoint_interect2.php
- hphp/hack/test/fanout/series_of_repo_changes/added_parent_disjoint_interect2.php.exp
- hphp/hack/test/fanout/series_of_repo_changes/added_parent_disjoint_interect3.php
- hphp/hack/test/fanout/series_of_repo_changes/added_parent_disjoint_interect3.php.exp
- hphp/hack/test/fanout/series_of_repo_changes/added_parent_disjoint_interect4.php
- hphp/hack/test/fanout/series_of_repo_changes/added_parent_disjoint_interect4.php.exp
