# Commit 017976

Commit hash: bfebe7805f6df35dcc88a156edecf2ff74d191f5
Commited at: 2024-10-09T10:10:44-07:00
Previous commit hash: 7ff6a6965e7c080ac5db7676972b14d3b0c5af0f
Next commit hash: aefc0eb71d5ec2889dc853ee55ac9885320720f5
Commit title: Allow ptr to map in folly MapUtils
Commit body:
Summary:
Allow passing a ptr into `folly::get_ptr(map, key)`.

There is some code that looks like:

```
auto foo(const MyMap* map) {
  if (map != nullptr && map.contains(k)) {
     auto& v = map.at(k);
     // do stuff with v
  }
}
```

That can now be re-written as:

```
auto foo(const MyMap* map) {
  if (const auto* vPtr = folly::get_ptr(map, k)) {
     auto& v = *vPtr;
     // do stuff with v
  }
}
```

Reviewed By: yfeldblum

Differential Revision: D64054668

fbshipit-source-id: 7ef52d5f4ef751ad796ba4a950b8358e14c7d0c7
Touched files:
- third-party/folly/src/folly/container/MapUtil.h
- third-party/folly/src/folly/container/test/MapUtilTest.cpp
