# Commit 018568

Commit hash: 312f3ddfd281a425aee273f3fe56c92975991e36
Commited at: 2024-10-29T08:32:39-07:00
Previous commit hash: 646d7e9c4af563f5a8c80c4846622695f4402657
Next commit hash: 7a24c6e54b6a944d0ec4e282f223ef63d8a80d16
Commit title: List.__eq__ doesn't raise on non-Sized
Commit body:
Summary:
In general, comparison operators shouldn't raise on a type mismatch; instead `__eq__` should return `False` and `__ne__` should return `True`. The behavior for `__lt__` etc. is ambiguous, so leaving that unchanged.

This diff adds a `Sized` check to return `False` early in equality check. It also splits the `list_compare` method into `list_eq` and `list_lt` to make these easier to understand and improve performance (e.g., less branching inside `for` loop).

Relative to the py3 implementation, `list_eq` is slightly worse because it has to accommmodate `self` is either `List` or `MutableList`, so no static type there.

V2:
#buildall

Reviewed By: yoney

Differential Revision: D64998004

fbshipit-source-id: c8cc7b83d0b2e58380ef3d07598ccc57be53afb8
Touched files:
- third-party/thrift/src/thrift/lib/py3/test/auto_migrate/lists.py
- third-party/thrift/src/thrift/lib/python/mutable_containers.pyx
- third-party/thrift/src/thrift/lib/python/test/lists.py
- third-party/thrift/src/thrift/lib/python/types.pxd
- third-party/thrift/src/thrift/lib/python/types.pyx
