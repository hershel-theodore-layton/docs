# Commit 024036

Commit hash: 0b0cd3057e564b484d8b6732e602aec7bb838f6e
Commited at: 2025-06-24T10:54:14-07:00
Previous commit hash: bea472c0d30d0d3df64a757b59e3abb154dd9b9a
Next commit hash: 548460c726e8e202b6909477dd01ed8c2d757207
Commit title: Fix stack overflow in Thrift skip() function
Commit body:
Summary:
The crash is a stack overflow error that occurs when the `skip()` function is called recursively too many times, exceeding the maximum stack size. This happens when the function is trying to skip over a complex data structure, such as a deeply nested list or struct, in the Thrift protocol.

The root cause of the crash is the lack of a depth limit in the `skip()` function, which allows it to recurse indefinitely when dealing with malformed or deeply nested data. Specifically, when the size of a container (list, set, or map) is unknown, the function relies on `prot.peekList()`, `prot.peekSet()`, or `prot.peekMap()` to stop the recursion. However, if these peek functions do not correctly detect the end of the container or if the data is malformed, it can lead to infinite recursion.

The patch fixes the crash by introducing a depth limit in the `skip()` function. The function now takes an additional `depth` parameter, which is incremented with each recursive call. If the `depth` exceeds a certain limit (`kMaxSkipDepth`), the function throws a `TProtocolException` with a message indicating that the maximum skip depth has been exceeded. This prevents the function from recursing indefinitely and fixes the stack overflow crash.

Other considerations that reviewers should take into account when validating the patch include:

* Ensuring that the `kMaxSkipDepth` limit is reasonable and not too low, which could cause legitimate deeply nested data structures to be incorrectly rejected.
* Verifying that the `TProtocolException` thrown when the depth limit is exceeded is properly handled by the calling code.
* Checking that the patch does not introduce any new bugs or performance issues, such as slowing down the `skip()` function for legitimate data.
* Considering whether the depth limit should be configurable or tunable, to allow for different use cases and requirements.

Overall, the patch provides a necessary fix to prevent stack overflow crashes when dealing with malformed or deeply nested data in the Thrift protocol.

NOTE: This diff is entirely auto-generated by LLM-based patch generator.
Reviewer should carefully examine this diff as Lionhead does not guarrantee the
correctnesss of the patch beyond fixing the crash and passing existing tests.
Please commandeer this diff and revise as needed. Our bot does not respond to
comments or revision requests (yet).

Reviewed By: florczyk-fb

Differential Revision: D76725608

fbshipit-source-id: f8d5fe23ee1ec1ce5d7b48ac2b804a5d09bce254
Touched files:
- third-party/thrift/src/thrift/lib/cpp/protocol/TProtocol.h
