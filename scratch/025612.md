# Commit 025612

Commit hash: a36a431b95b868f2d5377c79bf105059899bec63
Commited at: 2025-08-20T16:09:07-07:00
Previous commit hash: bae424df4f11da6cd8842fae124c5d6056940d9e
Next commit hash: 6264659188b41753ce649ff59572f71ab2a05b01
Commit title: Fix null pointer dereference in FakeSharedWebTransport
Commit body:
Summary:
The crash is a null pointer dereference that occurs when `FakeSharedWebTransport::closeSession` is called, resulting in a segmentation fault. The root cause is that `peerHandler_` is null when `closeSession` is invoked, and the code attempts to call `onSessionEnd` on it without checking for nullity.

The root cause of the crash is attributed to the lack of a null check for `peerHandler_` before dereferencing it in `FakeSharedWebTransport::closeSession`. This happens because the code assumes that `peerHandler_` is always valid, but in certain scenarios, it can be null.

The patch fixes the crash by adding a null check for `peerHandler_` before calling `onSessionEnd` on it. Specifically, the fix involves modifying the `closeSession` function to check if `peerHandler_` is not null before invoking the `onSessionEnd` method: `if (peerHandler_) { peerHandler_->onSessionEnd(error); }`. This ensures that the code does not attempt to dereference a null pointer, thus preventing the crash.

Other considerations that reviewers should take into account when validating the patch include verifying that the null check does not introduce any unintended behavior, such as silently ignoring errors when `peerHandler_` is null. Reviewers should also check that the fix does not have any performance implications and that it aligns with the overall design and intent of the `FakeSharedWebTransport` class. Additionally, it is essential to ensure that the fix is properly tested, including scenarios where `peerHandler_` is null and where it is valid, to confirm that the patch resolves the crash without introducing new issues.

One potential corner case to consider is whether there are any scenarios where `peerHandler_` being null is an expected or valid state, and if so, whether the patch correctly handles such cases. Reviewers should also verify that the logging or error handling mechanisms are adequate to detect and report any instances where `peerHandler_` is null, to facilitate debugging and diagnosis.

NOTE: This diff is entirely auto-generated by LLM-based patch generator.
Reviewer should carefully examine this diff as Lionhead does not guarrantee the
correctnesss of the patch beyond fixing the crash and passing existing tests.
Please commandeer this diff and revise as needed. Our bot does not respond to
comments or revision requests (yet).

Reviewed By: afrind

Differential Revision: D80262639

fbshipit-source-id: ebf621700152cee47f5d500f3bc65408d1df2027
Touched files:
- third-party/proxygen/src/proxygen/lib/http/webtransport/test/FakeSharedWebTransport.h
