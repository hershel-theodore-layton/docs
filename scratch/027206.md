# Commit 027206

Commit hash: 247ceca87e924ba4b993ec7bb09e1308b45eb18a
Commited at: 2025-10-14T12:29:47-07:00
Previous commit hash: b05eceba729fc262d0889e1479bf7b0ea3f84604
Next commit hash: c3bf730eb820a50bf4adb953ebce81d9d9344ed7
Commit title: Replace t_map::get_key/val_type with key/val_type().get_type() in Thrift compiler
Commit body:
Summary:
Replaced `map->get_key_type()` / `map->get_val_type()` with `map->key_type().get_type()` / `map->val_type().get_type()`.

`get_X_type` AST methods returning `t_type*` are deprecated and will be removed soon.

`get_X_type()` returns `t_type*` which is de-referenced. The corresponding `X_type()` returns `t_type_ref`, whose `operator*` de-references the contained pointer (i.e. is equivalent to dereferencing a `t_type*`.

`t_type_ref::get_type()` is also deprecated, but this unblocks the removal of `get_elem_type` and allows us to handle migrating `t_type_ref::get_type()` uniformly across the board.

Reviewed By: iahs

Differential Revision: D84544948

fbshipit-source-id: c318ed6b454ba9d84a842d3f1aad33fa8a730227
Touched files:
- third-party/thrift/src/thrift/compiler/generate/common.cc
- third-party/thrift/src/thrift/compiler/generate/cpp/util.cc
- third-party/thrift/src/thrift/compiler/generate/t_concat_generator.h
- third-party/thrift/src/thrift/compiler/generate/t_hack_generator.cc
- third-party/thrift/src/thrift/compiler/generate/t_mstch_py3_generator.cc
