# Commit 029631

Commit hash: 2e4bee1dd353f062d9c653bc714e302668b1b643
Commited at: 2026-01-21T12:57:01-08:00
Previous commit hash: dfd3d077df830a5bbb9b31071666b7fbeea67d51
Next commit hash: 62182e9058395932d90e7cb587b03e52492a9a20
Commit title: AsyncSocket: unset O_NONBLOCK when using io_uring
Commit body:
Summary:
# AsyncSocket io_uring

Currently, the only way to use io_uring for network I/O is to use `AsyncIoUringSocket`. Using a different socket type has several issues:

* Much of the implementation logic is duplicated.
* Code assumes `AsyncSocket` and...
  * Rely on its implementation details. Several Ucache issues with `AsyncIoUringSocket` were caused by subtle differences in implementation.
  * Using `AsyncIoUringSocket` means changing the callsites. Many places assume `AsyncSockets`.
  * Some callsites (e.g. peekers) can't use `AsyncIoUringSocket` and there's currently an awkward async API to use io_uring in `AsyncSocket`; see `ReadMode::ReadZC`.

Make `AsyncSocket` work with `IoUringBackend` natively. The main design goal is identical user facing behaviour.

# Motivation

io_uring is already async and will internally re-arm requests e.g. when sending a large buffer, but only when O_NONBLOCK is **not set**. If it is, then io_uring will obey and return EAGAIN in the completion, requiring a reissue and being less efficient.

Most sendmsg/recv/recvmsg syscalls already set MSG_DONTWAIT. This is generally more preferable than blanket setting O_NONBLOCK on the entire fd.

Unset O_NONBLOCK when the socket is connected and io_uring is used.

Reviewed By: vishwanath1306

Differential Revision: D90945165

fbshipit-source-id: a7cf66fa3451a98d750db3aba99a01951f53c411
Touched files:
- third-party/folly/src/folly/io/async/AsyncSocket.cpp
- third-party/folly/src/folly/net/NetOps.cpp
- third-party/folly/src/folly/net/NetOps.h
- third-party/folly/src/folly/net/NetOpsDispatcher.cpp
- third-party/folly/src/folly/net/NetOpsDispatcher.h
