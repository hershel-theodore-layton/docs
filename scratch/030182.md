# Commit 030182

Commit hash: 11c60ea68a47a7b0aa0d6c6cdf58a61a01c2284b
Commited at: 2026-02-09T15:04:44-08:00
Previous commit hash: 9d34cef4211acc6bb2a8dfbd52f0d5fff098842b
Next commit hash: 365f5abe92d60b20a6508596c8bda9fa20dfc859
Commit title: Get rid of the servlet dependency to avoid log4j "is webapp" mode
Commit body:
Summary:
this is causing log4j to run in "is webapp" mode

https://github.com/apache/logging-log4j2/blob/4f474b32751f4ccad67424ca585612584440cd63/log4j-api/src/main/java/org/apache/logging/log4j/util/Constants.java#L47
https://github.com/apache/logging-log4j2/blob/4f474b32751f4ccad67424ca585612584440cd63/log4j-api/src/main/java/org/apache/logging/log4j/util/Constants.java#L66

Everything I'm deleting here seems unused across the codebase.

For people reading this to understand the change to log4j (from https://logging.apache.org/log4j/2.x/javadoc/log4j-api/org/apache/logging/log4j/util/Constants.html):

> Specifies whether Log4j is used in a servlet container
> If true Log4j disables the features, which are incompatible with a typical servlet application:

> * It disables the usage of ThreadLocals for object pooling (unless the user has explicitly provided a ENABLE_THREADLOCALS property)
> * It uses a web-application safe implementation of ThreadContextMap (see Provider.getThreadContextMap()),
> * It disables the shutdown hook,
> * It uses the caller thread to send JMX notifications.
> The value of this constant depends upon the presence of the Servlet API on the classpath and can be overridden using the "log4j2.isWebapp" system property.

 ---

with this change `log4j2.is.webapp` (also `log4j2.isWebapp`) becomes `true` instead of `false`

> This system property can be used to force Log4j 2 to behave as if it is part of a web application (when true) or as if it is not part of a web application (when false).

 ---

`log4j2.enable.threadlocals` (also `log4j2.enableThreadlocals`) becomes `false` instead of `true`

> This system property can be used to switch off the use of threadlocals, which will partly disable Log4j's garbage-free behaviour: to be fully garbage-free, Log4j stores objects in ThreadLocal fields to reuse them, otherwise new objects are created for each log event. Note that this property is not effective when Log4j detects it is running in a web application.

 ---

`log4j2.jmx.notify.async` (also `log4j2.jmxNotifyAsync`)

> If true, log4j's JMX notifications are sent from a separate background thread, otherwise they are sent from the caller thread. If system property log4j2.is.webapp is true or the javax.servlet.Servlet class is on the classpath, the default behaviour is to use the caller thread to send JMX notifications.

 ---

https://logging.apache.org/log4j/2.12.x/manual/configuration.html

Reviewed By: j-bahr

Differential Revision: D92715607

fbshipit-source-id: 687a4a87670cbd1d91868d834d1b912155766157
Touched files:
- third-party/thrift/src/thrift/lib/javadeprecated/src/main/java/com/facebook/thrift/server/TServlet.java
