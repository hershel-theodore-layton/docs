# Commit 030679

Commit hash: 8d1f1d3cae00d1301a892fbdadfe8b0608284fc8
Commited at: 2026-02-23T15:37:45-08:00
Previous commit hash: dbfdefb57d1311ebb5aa13ee2277dc3b5c4f8b5c
Next commit hash: 09ed952925d6b00b5f8705025aeb1c36293f821e
Commit title: Migrate pyi_mstch_field to Whisker
Commit body:
Summary:
Migrate `field` properties to Whisker prototype.

Replace `PEP484Optional?` with base Whisker generator's `optional?` property. For every non-optional case, the return of `PEP484Optional?` is always false:

| Qualifier | `PEP484Optional?` | Notes |
| `optional` | `true` | `is_optional` is true, short-circuits the `||` |
| `required` | `false` | `!is_required` is false, so the right side of the `||` is false, and `is_optional` is also false |
| `terse`, `none` | `false` | `is_unqualified` is true, which forces `has_default_value` to true (via `has_value || is_unqualified`). Then `!has_default_value` is false, making the right side of the final `||` condition false, `is_optional` is also false |

Reviewed By: praihan

Differential Revision: D93933285

fbshipit-source-id: 25123353b2a664f421832d717a3d59c41b1edd00
Touched files:
- third-party/thrift/src/thrift/compiler/generate/t_mstch_pyi_generator.cc
- third-party/thrift/src/thrift/compiler/generate/templates/pyi/ttypes.pyi.mustache
