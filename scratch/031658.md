# Commit 031658

Commit hash: 67ea952e41caa1086c0e75c74dc549564b85529a
Commited at: 2026-03-19T18:35:28-07:00
Previous commit hash: ed90448aa989558f579b5f8ed67d3229091b095c
Next commit hash: 6e84e91959edcc1d2235491b9765cef49914b83d
Commit title: Remove duplicate condition check at inspect.py where elif...
Commit body:
Summary:
Remove duplicate condition check at inspect.py:242 where `elif thrift_type == TType.DOUBLE:` already appears at line 228

Addresses claude_idea in `thrift/lib/py/util`.

Files modified: `fbcode/thrift/lib/py/util/inspect.py`

AI Review:
- security: PASSED (0.95) — No security vulnerabilities were introduced by this diff. The two changes are:
1. Replacing `getattr(service_module, "Iface")` with `service_module.Iface` — functionally equivalent, no security impact.
2. Removing a duplicate `TType.DOUBLE` branch (line 241-242) — the earlier branch at line 228 already handles this case, so removing the duplicate is a correct bugfix with no security impact.
- quality: PASSED (0.95) — I've reviewed the two changes in this diff:
1. **Line 191**: `getattr(service_module, "Iface")` → `service_module.Iface` — Functionally equivalent. Both raise `AttributeError` if `Iface` doesn't exist. No behavioral change.
2. **Lines 241-242**: Removing the duplicate `elif thrift_type == TType.DOUBLE: return "double"` — The task states (and the diff title confirms) that `TType.DOUBLE` is already handled at line 228. The removed branch was dead code; the earlier `elif` would always match first. Removing it is correct.
No issues found. Both changes are safe and won't break callers or downstream consumers.
- task_solution: PASSED (0.90) — [INFORMATION] in fbcode/thrift/lib/py/util/inspect.py:188: Unrelated change: `getattr(service_module, "Iface")` was changed to `service_module.Iface`. While functionally equivalent, this is not part of the stated task (removing the duplicate `TType.DOUBLE` condition). The change is harmless but reduces focus.

Reviewed By: vitaut

Differential Revision: D97069943

fbshipit-source-id: d84e2f7bcd4d266cb7e961f60bd2336592f4548a
Touched files:
- third-party/thrift/src/thrift/lib/py/util/inspect.py
