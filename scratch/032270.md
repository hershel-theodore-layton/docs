# Commit 032270

Commit hash: 02cc4ff7204fc8518d3c41566bec050e2373dee5
Commited at: 2026-04-16T00:47:42-07:00
Previous commit hash: d38af18ae82ff8a663d26087a12eb46fb1e7291d
Next commit hash: 8144aa3818b423b7e1bae8c36f2e995dbb5a40d8
Commit title: Replace usages of deprecated AST method t_type_ref::get_type in concat generators
Commit body:
Summary:
Replace usages of deprecated method `t_type_ref::get_type`.

In generators, `t_type_ref` will always be either `empty` or `resolved`, because the Thrift compiler does not invoke the generator for ASTs that don't pass semantic validation.

For resolved typedefs, we can translate `get_type` into the following using `t_type_ref`'s `deref`, `operator*` and `operator->`:

| Before | After |
| --- | --- |
| `t_type* = ref.get_type()` | `t_type* = &ref.deref()` |
| `t_type& = *ref.get_type()` | `t_type& = *ref` |
| `ref.get_type()->foo()` | `ref->foo()` |

For truly nullable cases (where the type ref might be empty/unresolved, due to being optional - e.g. `t_function::interaction`), we can translate `ref.get_type()` to the equivalent `t_type* = ref.resolved() ? &ref.deref() : nullptr`.

___

Differential Revision: D101096216

fbshipit-source-id: 20099af4808cd739805e1e691efa189d1bcf4e67
Touched files:
- third-party/thrift/src/thrift/compiler/generate/t_cocoa_generator.cc
- third-party/thrift/src/thrift/compiler/generate/t_concat_generator.h
- third-party/thrift/src/thrift/compiler/generate/t_json_generator.cc
- third-party/thrift/src/thrift/compiler/generate/t_py_generator.cc
