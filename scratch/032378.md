# Commit 032378

Commit hash: 5bd997dd2b4c90d4a86ff317d03c4c3bdb162ff5
Commited at: 2026-04-20T07:34:52-07:00
Previous commit hash: 9a1819814ad6301fa8e35bddf417c9b07b1126e8
Next commit hash: f950ad6cf902fa6117081d1bc44fdd79f9ad8dcc
Commit title: Replace usages of and remove deprecated AST method t_type_ref::get_type
Commit body:
Summary:
Replace remaining usages of deprecated method `t_type_ref::get_type` and remove the method.

In AST accessors, the type ref may yet be unresolved, so we need to explicitly validate.

In generators, `t_type_ref` will always be either `empty` or `resolved`, because the Thrift compiler does not invoke the generator for ASTs that don't pass semantic validation.

For resolved typedefs, we can translate `get_type` into the following using `t_type_ref`'s `deref`, `operator*` and `operator->`:

| Before | After |
| --- | --- |
| `t_type* = ref.get_type()` | `t_type* = &ref.deref()` |
| `t_type& = *ref.get_type()` | `t_type& = *ref` |
| `ref.get_type()->foo()` | `ref->foo()` |

For truly nullable cases (where the type ref might be empty/unresolved, due to being optional - e.g. `t_function::interaction`), we can translate `ref.get_type()` to the equivalent `t_type* = ref.resolved() ? &ref.deref() : nullptr`.

Reviewed By: dtolnay

Differential Revision: D101100274

fbshipit-source-id: 2951ab4faf6cb06b7dab75df9d138c672f93b8eb
Touched files:
- third-party/thrift/src/thrift/compiler/ast/t_const.h
- third-party/thrift/src/thrift/compiler/ast/t_type.h
- third-party/thrift/src/thrift/compiler/ast/t_typedef.h
- third-party/thrift/src/thrift/compiler/codemod/structure_annotations.cc
- third-party/thrift/src/thrift/compiler/generate/python/util.cc
- third-party/thrift/src/thrift/compiler/generate/t_hack_generator.cc
- third-party/thrift/src/thrift/compiler/sema/explicit_include_validator.cc
- third-party/thrift/src/thrift/compiler/sema/schematizer.cc
- third-party/thrift/src/thrift/compiler/sema/sema.cc
- third-party/thrift/src/thrift/compiler/sema/standard_validator.cc
- third-party/thrift/src/thrift/compiler/test/t_typedef_test.cc
