# Commit 032645

Commit hash: bb6a9d2d69d81e123143c28e608da9b6f96de3aa
Commited at: 2026-04-28T06:18:23-07:00
Previous commit hash: b9c11599edd77fbdd3fa02e43eac68b6bbe1230a
Next commit hash: c29d45e21f1ab9cffb8220f4e88f7a8ad85daf7c
Commit title: BiDi: server function signatures
Commit body:
Summary:
D102002988 as reference:
```
# Thrift schema
service ChatService {
  sink<ClientMessage>, stream<ServerMessage> Chat(1: ChatRoom room);
}
```
```
# Go Handler code
func (h *Handler) Chat(ctx context.Context, room *ChatRoom) (func(context.Context, iter.Seq2[*ClientMessage, error], chan<- *ServerMessage) error, error) {

    bidiFunc := func(ctx context.Context, seq iter.Seq2[*ClientMessage, error], streamChan chan<- *ServerMessage) error {
        for msg, err := range seq {
            if err != nil {
                // Client side error - clean up and return
                return err
            }
            // Process client message and send response
            response := ProcessMessage(msg)
            select {
            case <-ctx.Done():
                return ctx.Err()
            case streamChan <- response:
            }
        }
        return nil
    }
    return bidiFunc, nil
}
```

Supports:
* Initial response (optional - if Thrift schema defines it)
* Initial error
* Sink elements (client -> server) via `iter.Seq2[SinkElemType, error]`
* Stream elements (server -> client) via `chan<- StreamElemType`
* Sink and stream declared exceptions
* Method-level declared exceptions
* BiDi is done by a user-defined `func` object.
    * The function will be invoked by Thrift library logic outside of codegen (to be implemented downstream).
    * `Context` object *should* be used for detecting stream interruption (e.g. client disconnected) and gracefully cleaning-up/terminating.
    * `iter.Seq2` should be used to consume sink elements from the client.
    * `chan<-` should be used to produce stream elements to the client.
    * Users *should* follow recommended practices above to avoid Go-routine or other resource leaks.

#build_rule_type[go_library,go_binary,go_test]

Reviewed By: podtserkovskiy

Differential Revision: D102042896

fbshipit-source-id: 7bcf79b81bdd652d8b14a447b41b2a8051dd8489
Touched files:
- third-party/thrift/src/thrift/compiler/generate/go/util.cc
- third-party/thrift/src/thrift/compiler/generate/templates/go/svc/handler.whisker
- third-party/thrift/src/thrift/compiler/generate/templates/go/svc/processor.whisker
- third-party/thrift/src/thrift/compiler/test/fixtures/bidi/out/go/gen-go/metadata.go
- third-party/thrift/src/thrift/compiler/test/fixtures/bidi/out/go/gen-go/svcs.go
- third-party/thrift/src/thrift/conformance/go/rpc_server.go
