# Commit 032814

Commit hash: 9a2e4a9deb3ed073cd01454a77b2d67fb7c63a78
Commited at: 2026-05-04T14:38:44-07:00
Previous commit hash: a6e7d88604aa9a7f46f63606e5eed1a509dacffd
Next commit hash: 544f6191e6ffca95129d18f5d2a9638f7e5b1103
Commit title: Fix rust 1.95 sysroot, third-party, and test breakages
Commit body:
Summary:
- Fix the xplat 1.95.0 sysroot import fallout by updating the std broken-pipe overlay to match upstream names and by teaching `rust_third_party.bzl` to forward reindeer's newer `local_manifest_dir` parameter.
- Update Buck's Rust custom-target helpers for 1.95.0 by adding `-Zunstable-options` and `RUSTC_BOOTSTRAP=1` handling to the `rustc --print cfg` and clippy `rustc --print sysroot` probes when `RUST_TARGET_PATH` is in use.
- Fix third-party Rust 1.95 compatibility in vendored crates by updating `bytecount`'s SIMD mask conversion path to the current portable-SIMD API, moving `bytecount` and `stabby-abi` source changes into proper reindeer overlays, and adding a `rustversion` overlay that promotes Meta's bootstrap-stable rustc to nightly under `RUSTC_BOOTSTRAP=1`, so `bytemuck`'s date-gated portable-SIMD `Pod` and `Zeroable` impls activate again.
- Restore APSW to the original bytemuck-checked representation path by reverting `lightpipe` back to `bytemuck::must_cast` and `volatile_view::SafeToView: bytemuck::Pod`, with targeted rustversion/bytemuck coverage for that path.
- Update first-party source compatibility for 1.95.0 by switching first-party `assert_matches` imports and remaining std/core macro paths to `use std::assert_matches;` and removing obsolete `if_let_guard` feature gates that are now stable.
- Make rustdoc search-index parsing follow rustdoc 1.95's `EntryData` schema exactly, including `unstable` and `associated_item_disambiguator`, instead of ignoring trailing fields.
- Restore the missing `adt_const_params` companion gate in xtensa graph code and fix the xplat locomotion mirror Buck deps/autodeps shape so `check_all` no longer resolves shared `//folly/...` and `//caffe2/...` labels as missing `fbsource//...` packages.
- Fix xplat wa-voip CLI loading and native-client linking by keeping the CLI-owned `WESSessionStoreImpl*` implementation and linking `core_system` whole, instead of pulling in the duplicate `SessionStore_Dictionary` plugin implementation.
- Fix the Windows `haptics_sdk_napi` cdylib build under `@//arvr/mode/win/clang/dev` by overriding the conflicting CRT import library selection in the target BUCK so lld-link no longer trips duplicate `__vcrt_InitializeCriticalSectionEx` symbols.

Differential Revision: D102419143

fbshipit-source-id: 0804e6230a05597fc2b7b29dd36c22c010aa5662
Touched files:
- hphp/hack/src/utils/rust/file_rwlock.rs
