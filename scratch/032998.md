# Commit 032998

Commit hash: e7c5188623e7b6ea1f8d35d4a6ca935714206463
Commited at: 2026-05-09T21:32:20-07:00
Previous commit hash: 7b32d98aac3ad938e96a93a4815533d25e2bc7fd
Next commit hash: 3d3d5f35c9435a1bc8c25ea13bc6fbb38f74e894
Commit title: AsyncIoUringSocket: cancel in-flight FastOpenSqe in destructor to prevent UAF
Commit body:
Summary:
This is the fix for the regression test added in the parent diff.

# Production crash

A SIGSEGV is firing in production hyperclient (BET TW jobs) at
`fbcode/folly/io/async/AsyncIoUringSocket.cpp:1528` inside
`AsyncIoUringSocket::WriteSqe::callback`. The crashing instruction is
`parent_->writeSqeActive_ = nullptr;` — the `WriteSqe`'s raw `parent_`
pointer is being dereferenced after the parent `AsyncIoUringSocket` has
been freed.

Reference: https://fburl.com/scuba/coredumper/y3r65k7m

https://www.internalfb.com/phabricator/paste/view/P2316734161?view=markdown

# Bug deep-dive

`~AsyncIoUringSocket()` (`AsyncIoUringSocket.cpp:155-200`) cancels the
in-flight `readSqe_` (lines 162-168), `closeSqe_` (170-175),
`connectSqe_` (176-180), and `writeSqeActive_` (185-192) — but it does
**not** cancel `fastOpenSqe_`. The `unique_ptr` simply destructs
in-place. Since `cancel()` was never called, the kernel still holds an
in-flight SQE whose `user_data` points at the freed `FastOpenSqe`. When
the late TFO CQE arrives:

1. `IoUringBackend::internalProcessCqe` reads the freed `FastOpenSqe` to
   dispatch the callback.
2. `FastOpenSqe::callback` (`AsyncIoUringSocket.h:476-478`) calls
   `parent_->processFastOpenResult(cqe)`.
3. On a successful TFO CQE, `processFastOpenResult` at
   `AsyncIoUringSocket.cpp:459-465` runs:
   ```
   processConnectResult(&tmp);
   writeSqeActive_ = fastOpenSqe_->initialWrite.release();
   writeSqeActive_->callback(cqe);
   ```
4. That last call drops directly into `WriteSqe::callback`, which writes
   `parent_->writeSqeActive_ = nullptr;` at line 1528 against a
   `WriteSqe::parent_` (`AsyncIoUringSocket.h:414`) that points at the
   freed `AsyncIoUringSocket`.

The destructor handles the analogous case for `connectSqe_`
(`AsyncIoUringSocket.cpp:176-180`) symmetrically; `fastOpenSqe_` was
simply omitted. Hyperclient is the production trigger because it
enables TFO on outbound Thrift connections, putting `fastOpenSqe_` on
the in-flight path.

# What the fix does

Insert one symmetric block in `~AsyncIoUringSocket()` between the
existing `connectSqe_` and `writeSqeActive_` cancel paths:

```cpp
if (fastOpenSqe_ && fastOpenSqe_->inFlight()) {
  VLOG(3) << "cancel fastopen " << fastOpenSqe_.get();
  backend_->cancel(fastOpenSqe_.release());
}
```

`backend_->cancel()` (`IoUringBackend.cpp:1397-1408`) marks the SQE
cancelled and takes ownership; when the late CQE arrives,
`IoSqeBase::internalCallback` (`IoUringBackend.cpp:417-433`) routes to
`FastOpenSqe::callbackCancelled` (`AsyncIoUringSocket.h:480-482`) which
self-deletes the `FastOpenSqe`. The embedded
`unique_ptr<WriteSqe> initialWrite` member is destructed with it; that
`WriteSqe` was never independently submitted (it is consumed only by
`FastOpenSqe::processSubmit` at `AsyncIoUringSocket.cpp:1323-1326`), so
its destruction is safe.

No header changes required.

Differential Revision: D104532765

fbshipit-source-id: 66f9d63f3f5e934dd2810ec7ae595c4907b8122e
Touched files:
- third-party/folly/src/folly/io/async/AsyncIoUringSocket.cpp
